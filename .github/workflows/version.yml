name: Atualizar Versão

on:
  push:
    branches:
      - main

jobs:
  atualizar-versao:
    runs-on: ubuntu-latest

    steps:
      - name: Verificar código
        uses: actions/checkout@v2

      - name: Configurar Java
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '17'

      - name: Atualizar Versão
        run: |
          cd $GITHUB_WORKSPACE
          chmod +x version.sh
          ./version.sh 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

      - name: Commit e Push
        run: |
            git add .
            git commit -m "Atualizando versão POM"
            git push origin main